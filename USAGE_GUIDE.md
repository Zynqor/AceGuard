# 📋 完整使用指南

## 🎯 快速查看Actions构建状态

### 方法1：通过GitHub网页

1. 访问你的仓库页面：
   ```
   https://github.com/Zynqor/AceGuard
   ```

2. 点击顶部的 **"Actions"** 标签

3. 你会看到工作流运行列表，最新的应该是：
   ```
   ✓ Update workflow to trigger on branch push for testing
   ```

4. 点击进入可以查看详细的构建日志

5. 构建成功后，在工作流页面底部可以看到 **"Artifacts"** 部分：
   - 下载 `AceGuard-Windows-x64-dev-YYYYMMDD-HHMMSS.zip`

### 方法2：直接链接

Actions页面直接链接：
```
https://github.com/Zynqor/AceGuard/actions
```

## 📦 如何下载构建好的程序

### 当前（开发版本）

由于刚刚推送的是分支（不是标签），构建产物会以 **Artifacts** 形式保存：

1. 进入 Actions 页面
2. 点击最新的成功构建
3. 滚动到底部的 "Artifacts" 部分
4. 下载 `AceGuard-Windows-x64-dev-xxxxx`
5. 解压后就可以使用

**注意**：Artifacts 会在90天后自动删除。

### 正式发布（Release版本）

要创建永久的Release版本，需要推送标签。但由于Git配置限制，建议通过GitHub网页操作：

#### 方法A：通过GitHub网页创建Release（推荐）

1. 访问仓库的 Releases 页面：
   ```
   https://github.com/Zynqor/AceGuard/releases
   ```

2. 点击 **"Draft a new release"**

3. 填写信息：
   - **Tag version**: `v1.0.0`
   - **Target**: 选择你的分支 `claude/process-affinity-priority-tool-011CUrEAXg87phcLHNGnvgQT`
   - **Release title**: `v1.0.0 - 首次发布`
   - **Description**: 复制下面的内容

4. 发布后，GitHub Actions会自动构建并上传文件到Release

#### Release描述模板：

```markdown
## AceGuard 进程资源限制工具 v1.0.0

### 📦 下载说明

**推荐下载：** `AceGuard-Windows-x64.zip` (包含完整文件和文档)

或者单独下载：
- `AceGuard.exe` - GUI图形界面版本（推荐）
- `AceGuard-CLI.exe` - 命令行版本

### ✨ 功能特性

#### GUI图形界面版本
- ✅ 直观的图形界面，简单易用
- ✅ 进程列表管理（添加/编辑/删除）
- ✅ 实时日志显示
- ✅ 系统托盘最小化
- ✅ 开机自启动功能
- ✅ 配置文件自动保存/加载

#### 核心功能
- ✅ 设置进程CPU亲和性（限制使用的CPU核心）
- ✅ 设置进程优先级（控制CPU时间分配）
- ✅ 持续监控模式，自动重新应用设置
- ✅ 支持多进程同时管理

### 🚀 快速开始

1. 下载 `AceGuard-Windows-x64.zip`
2. 解压到任意目录
3. **右键点击 `AceGuard.exe`，选择"以管理员身份运行"**
4. 在图形界面中添加要限制的进程：
   - 进程名称：例如 `AceGuard.exe`
   - CPU核心：例如 `0,1` （只使用前2个核心）
   - 优先级：推荐 `IDLE` 或 `BELOW_NORMAL`
5. 点击 "▶️ 开始监控" 按钮

### ⚠️ 重要警告

**使用本工具修改反作弊程序（如AceGuard、EasyAntiCheat等）可能导致游戏账号被封禁！**

本工具仅供学习研究使用，使用者需自行承担所有风险。

详细风险说明请查看压缩包内的 `WARNINGS.md` 文件。

### 📖 文档说明

压缩包内包含：
- `README.md` - 完整使用说明
- `WARNINGS.md` - 详细的安全警告和风险说明
- `EXAMPLES.md` - 25个实用示例
- `config.json` - 配置文件模板
- `LICENSE` - MIT许可证

### 💻 系统要求

- Windows 7/8/10/11 (64位)
- 管理员权限
- **无需安装Python环境**

### 🔧 使用示例

#### 限制反作弊程序

```json
{
  "processes": [
    {
      "name": "AceGuard.exe",
      "affinity": [0, 1],
      "priority": "IDLE"
    }
  ]
}
```

这将：
- 限制程序只使用CPU核心0和1（共2个核心）
- 设置为最低优先级（仅在系统空闲时运行）
- 有效降低资源占用

### 📝 更新日志

**v1.0.0** - 2025-11-06

初始发布版本：
- GUI图形界面版本
- 命令行版本
- 完整的文档和示例
- 系统托盘和开机自启动功能

### 🐛 问题反馈

如遇到问题，请在 Issues 页面反馈：
https://github.com/Zynqor/AceGuard/issues

### 📄 许可证

MIT License - 详见 LICENSE 文件
```

#### 方法B：手动上传文件（如果Actions没有自动上传）

如果GitHub Actions没有自动上传文件到Release，可以手动上传：

1. 从Actions下载Artifacts
2. 解压得到：
   - `AceGuard.exe`
   - `AceGuard-CLI.exe`
   - `AceGuard-Windows-x64.zip`
3. 在Release编辑页面，拖拽这些文件到 "Attach binaries" 区域
4. 点击 "Publish release"

## 🎨 GUI界面使用说明

### 启动程序

1. **以管理员身份运行** `AceGuard.exe`
2. 程序会自动检查权限，如果不是管理员会提示

### 添加进程

1. 点击左下角的 **"➕ 添加"** 按钮
2. 在弹出的对话框中填写：
   - **进程名称**: `AceGuard.exe` （或其他要限制的程序）
   - **CPU核心**: `0,1` （表示只使用核心0和1）
   - **优先级**: 选择 `IDLE` 或 `BELOW_NORMAL`
3. 点击 **"确定"**

### 开始监控

1. 设置 **"监控间隔"**（默认5秒，表示每5秒检查一次）
2. 点击 **"▶️ 开始监控"** 按钮
3. 程序会自动：
   - 查找指定的进程
   - 应用CPU亲和性和优先级设置
   - 在日志中显示结果

### 其他功能

- **🔄 立即应用**: 立即应用一次设置（不启动持续监控）
- **📌 最小化到托盘**: 隐藏到系统托盘后台运行
- **设置 → 开机自启动**: 启用后系统启动时自动运行
- **✏️ 编辑**: 修改已添加的进程配置
- **🗑️ 删除**: 删除不需要的进程配置

### 日志查看

右侧的 **"运行日志"** 区域会实时显示：
- 进程检测状态
- 设置应用结果
- 错误信息（如果有）

示例日志：
```
[14:30:15] 检测到进程 AceGuard.exe: [1234]
✓ 成功设置进程 1234 的CPU亲和性: 核心 [0, 1]
✓ 成功设置进程 1234 的优先级: IDLE
```

## 🔄 监控模式说明

### 为什么需要持续监控？

反作弊程序可能会：
- 定期检查并恢复自己的优先级
- 检测到修改后重置设置
- 每次重启后恢复默认值

持续监控模式可以：
- 每隔几秒自动重新应用设置
- 确保设置始终生效
- 无需手动干预

### 监控间隔建议

- **5秒**（默认）- 平衡性能和响应速度
- **3秒** - 更快响应，略微增加CPU使用
- **10秒** - 降低性能影响，但响应较慢

## ⚙️ 配置文件说明

程序会自动保存配置到 `config.json`：

```json
{
  "processes": [
    {
      "name": "AceGuard.exe",
      "affinity": [0, 1],
      "priority": "IDLE"
    },
    {
      "name": "EasyAntiCheat.exe",
      "affinity": [0, 1, 2, 3],
      "priority": "BELOW_NORMAL"
    }
  ]
}
```

你可以：
- 直接编辑此文件
- 通过GUI添加/修改配置
- 备份此文件以便恢复

## 🚀 开机自启动设置

### 启用步骤

1. 在GUI中：点击菜单 **"设置 → 开机自启动"**
2. 确认对话框中点击 **"是"**
3. 程序会添加到Windows启动项

### 启用后行为

- Windows启动时自动运行程序
- 自动最小化到系统托盘
- 如果上次正在监控，会自动开始监控

### 禁用步骤

1. 再次点击 **"设置 → 开机自启动"**
2. 确认禁用

## 📱 系统托盘使用

### 最小化到托盘

- 点击GUI中的 **"📌 最小化到托盘"** 按钮
- 或直接关闭窗口并选择"最小化到托盘"

### 托盘菜单

右键点击托盘图标可以：
- **显示主窗口** - 恢复GUI界面
- **开始监控** - 启动监控（如未启动）
- **停止监控** - 停止监控
- **退出** - 完全退出程序

## 🛠️ 常见问题

### Q: 提示"需要管理员权限"？

A: 必须 **右键点击程序，选择"以管理员身份运行"**

### Q: 设置后没有效果？

A:
1. 确认进程名称正确（区分大小写）
2. 确认进程正在运行
3. 启用 "持续监控模式"
4. 查看日志区域的错误信息

### Q: 程序被杀毒软件拦截？

A:
- 这是正常的（因为请求管理员权限）
- 添加到白名单即可
- 源码完全开放，可以自己编译

### Q: 会被游戏检测吗？

A:
- **有可能！** 反作弊程序可能检测到设置被修改
- **可能导致封号！**
- 建议仅在测试环境使用

### Q: 如何卸载？

A:
1. 禁用开机自启动（如果启用了）
2. 直接删除程序文件夹即可
3. 无需卸载，不写注册表

## 📞 获取帮助

- **查看文档**: `README.md`, `EXAMPLES.md`, `WARNINGS.md`
- **提交Issue**: https://github.com/Zynqor/AceGuard/issues
- **查看源码**: https://github.com/Zynqor/AceGuard

## ⚖️ 免责声明

本工具仅供教育和研究目的使用。作者不对使用本工具造成的任何后果负责，包括但不限于游戏账号被封禁、系统不稳定、数据丢失等。

**使用者需自行评估风险并承担所有后果！**

---

祝使用愉快！如有问题欢迎反馈。
